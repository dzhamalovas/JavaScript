// Удаление дубликатов из массива. Создайте массив и удалите из него все
// повторяющиеся элементы.
let numbers = [5, 15, 5, 33, 15];
let duplNumbers = [...new Set(numbers)]; // Оператор распространения [...] (преобразование в массив)
console.log('Массив без дубликатов:', duplNumbers);


